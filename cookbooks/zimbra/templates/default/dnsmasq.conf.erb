server=8.8.8.8
server=8.8.4.4

listen-address=127.0.0.1,<%= node['ipaddress'] %>

domain=<%= node['domain'] %>

host-record=<%= node['fqdn'] %>,<%= node['ipaddress'] %>

mx-host=<%= node['domain'] %>,<%= node['fqdn'] %>,1

addn-hosts=/etc/hosts

cache-size=9500
